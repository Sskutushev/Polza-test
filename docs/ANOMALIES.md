# ANOMALIES

This file is the place for findings from `review.csv`.

Run:

```bash
pnpm profile:csv -- --file ./data/review.csv
```

Each finding should include the number of affected rows, the detection command, impact, and the chosen quarantine rule.
